int a, b;
int c, d;
int e, f;
float g;
int x, y, z;
if (a < b || (c < d && e < f)) {
    z = x + y;
} else {
    z = x * y;
}
z = x + y;
if (a > b && e > f) {
    z = a + b;
}
z = x * y;
while (z < 100) {
    z = z + 1;
}
z = z + 100;
